# 포트폴리오 전면 개편 작업 문서
## ▶ 이어서 시작하기 (새 세션용 인수인계 · 2026-07-27 기준)
- 작업 위치: `개인_Resume/portfolio-repo`, 브랜치 `redesign-editorial` (main=현 배포본, 손대지 말 것)
- 로컬 프리뷰: `cd portfolio-repo/redesign && python3 -m http.server 8742` → home.html / case-railink.html
- 헤드리스 검증: `chrome --headless=new --screenshot` (Theo는 CLI라 화면 못 봄 → 반드시 스크린샷 찍어 확인 후 보고)
- 완료: 0 세팅 / 1 그리드시스템(grid.css) / 2 홈 SPA(home.html) / **3 케이스 6종 전부**(railink·deokhong·samsung·openpath·genesis·korail) + 공통 조판 CSS(case.css) / 산출물(deliverables.html) / 디자인 시스템(design-system.html, 개편 시스템 기준으로 재작성)
- 페이지 전체: home.html(Index·Work·About·Contact SPA) + case-*.html 6 + deliverables.html + design-system.html. 메뉴는 6항목.
- **다음 할 일: 4 이미지 경량화(WebP)·성능·접근성 → 5 모바일 → 6 최종 검수 후 main 머지(G2)**
- 조판 공통 규칙은 `case.css`에 있음. 케이스별 1회성 그래프만 각 파일 `<style>`에.
  공통 컴포넌트: sec/cols/single · split+fig+ftile · summary(sm-row) · compare · flow · voices · shots(필름스트립) · figwide/figpair · metrics · next · reveal
- 케이스 순서(홈 인덱스와 동일): 01 RaiLink → 02 deokhong → 03 Samsung → 04 OPENPATH → 05 Genesis → 06 Korail → (다시 01). 홈 카드·Work 목록·각 케이지 하단 Next가 모두 이 순서로 연결됨.
- 원본 파일 경로는 repo 루트의 `index.html` (문서 다른 곳의 "site/index.html" 표기는 같은 파일을 가리킴)
- 헤드리스 캡처 주의: macOS Chrome은 창 최소 폭이 있어 `--window-size=390`으로 찍으면 실제로는 더 넓게 렌더된 걸 390으로 잘라 낸다(글자 잘림처럼 보임). 모바일 확인은 430 이상으로 찍거나, iframe(width:430)에 물려서 캡처할 것. 실측 결과 케이스 페이지는 430px에서 가로 넘침 없음(scrollWidth=430).
- 절대 원칙: (1) 형식만 개편, 내용/수치/그래프 창작·변경 금지. (2) 그리드 = 메타 col1-2, 콘텐츠 col3+ 여백 안. (3) 저해상 이미지 금지, 2x+ 재캡처. (4) 모션은 apple-design/emil 원칙(강한 ease-out·transform/opacity·reduced-motion).
- 원본 케이스 데이터 출처: `site/index.html` (각 케이스 article[data-work]). deokhong/Samsung/OPENPATH/Genesis/Korail 섹션·그래프를 여기서 추출.


2026-07-24 시작. 며칠에 걸쳐 이 브랜치(redesign-editorial)에서 진행하고, 단계마다 커밋으로 저장한다.
main(현재 배포본)은 개편이 끝나 승인될 때까지 건드리지 않는다.

## 확정 사항

1. **키비주얼: 프로젝트 카드 3D 캐러셀** (`redesign/hero.html`)
   - Theo 확정 수치: 카드 16vw 정사각 · 간격 1.24 · 원근 4000 · 카메라 높이 -40% · 속도 0.17 · 세로 위치 1vh · 벌어짐 1.5°
   - 검은 무대, 드래그 회전(관성)+세로 틸트, 뒤 카드 DOF 블러, 자동 회전
   - 카피 락업은 좌하단: 헤드라인(세리프) "Design less, solve more." + 바디 "The more complex the problem, the more it calls for subtraction."
   - 수치 재조정은 `hero.html?tune` 패널로
2. **내비게이션·모션: julianjakob.at 방식** (CSS 실측)
   - 스크롤 나열이 아니라 **페이지 전환(SPA)**: `.page` 컨테이너들이 `transform:scaleY()`로 세로 셔터처럼 열리고 닫힘
   - 모션 토큰: 페이지 전환 `.85s cubic-bezier(.65,0,.20,1)` / 오버레이 `.7s cubic-bezier(.80,0,.20,1)` / 기본 `.3s` · 빠름 `.2s` · 느림 `.4s`
   - 우상단 트리거 텍스트가 Menu ↔ Close로 바뀌고, 전체화면 검정 오버레이가 세로로 열림. 메뉴 항목은 `calc(100vw/6)` 들여쓰기, 호버 시 나머지 항목이 흐려짐
   - 라우팅: `#/`, `#/work`, `#/about`, `#/contact` (케이스는 `#/case/{슬러그}` 예정)
3. **그리드 레퍼런스: julianjakob.at** (CSS 실측 분석 완료)
   - 12컬럼 뷰포트 그리드: 오프셋을 `100vw/12` 단위로 계산 (`calc(100vw/6)` = 2컬럼 들여쓰기)
   - 본문 블록은 2컬럼 들여쓰기, 첫 컬럼은 메타 정보(작은 라벨) 열
   - CSS 변수 체계: `--ui-padding-side`, `--ui-padding-top` 식의 UI 토큰
   - 넉넉한 상하 여백: `clamp(80px, 11vh, 150px)`
4. **본문 형식: 블로그형 → 편집디자인형**
   - 현행 케이스 페이지(제목+문단 나열)를 버리고, 잡지 스프레드처럼 그리드 위에 텍스트·이미지·수치를 배치

## 단계 계획 (각 단계 끝에 커밋)

- [x] 0. 작업장 세팅: 브랜치, 확정 히어로 반입, 레퍼런스 분석
- [ ] 1. 그리드 시스템 정의: 12컬럼 토큰, 타이포 스케일, 다크/페이퍼 전환 규칙
- [x] 2. 홈 구성: 페이지 전환 SPA(Index/Work/About/Contact) + 메뉴 오버레이, 흑백 팔레트 확정
- [~] 3. RaiLink 케이스 편집 v2(2단 조판+마진+수치 타일+내용 복원) 완료, 카드→케이스 링크 연결. Theo 승인 후 나머지 5종 전개
- [x] 3-2. 나머지 케이스 5종 전개(deokhong·Samsung·OPENPATH·Genesis·Korail) + 공통 CSS(case.css) 분리
- [ ] 4. 이미지 경량화(WebP), 성능·접근성·KO/EN 점검
      · **남은 화질 숙제**: 과거 클라이언트 작업(Samsung·OPENPATH·Genesis·Korail)은 라이브 재캡처가 불가(사이트가 이미 바뀜).
        원본 자산이 546~640px뿐이라 표시 폭을 절반 이하로 제한해 2x 밀도를 맞춰 둠. 더 선명하게 하려면 Figma/원본 파일에서 재추출 필요.
      · KO/EN: 원본 index.html은 이중 언어인데 개편 케이지는 KO만 이식됨. EN 병기는 별도 작업.
- [ ] 5. 모바일 대응
- [ ] 6. 최종 검수 → main 머지 → 배포 (G2 승인 필수)


## 편집디자인 레퍼런스 (Figma, Theo 제공)
- Figma S9aG7F29Mlu5TvCQchwFMe node 793-24 = 파타고니아/MS 지속가능성 리포트 무드보드(구현대상 아님, 조판 참고)
- 규칙: **본문은 2단 조판 + 좌측 정렬 + 우측 여백**(풀블리드 아님). 풀블리드는 오프닝 사진 한 곳만. 큰 수치는 반투명/솔리드 색 타일로 이미지에 겹침. 고품질 사진. 다단 밀도 높은 본문.

## ★ 원칙 정정 (Theo, 2026-07-26): 이 개편은 형식(FORMAT)만 바꾼다
내용·데이터·그래프는 원본(site/index.html)이 진실의 원천. 재작성·수치변경·창작 금지. 레이아웃/타이포/흑백/모션만 바꾼다.
- 내가 어긴 것: 근무표 다이어그램을 근무코드까지 지어냄(제거), 진단 깔때기 데이터를 32로 바꿈(원복: 방문400/가입44/업로드29/활성23), 발견·전략·실험·지금 4개 섹션 통째 누락(복원), 카피 재작성(원문 복원).
- RaiLink 케이스는 원본 7섹션(문제·만든것·진단·발견·전략·실험·지금)+3그래프(깔때기·게이지0.3·스파크193) 전부 원문 그대로 이식 완료.

## (보류) 케이스 대표 비주얼 = 코드 벡터 다이어그램 (옵션 B)
Theo가 "형식만 바꾸라"고 정정 → 대표 비주얼 창작은 보류. 오프닝은 지어낸 다이어그램 대신 타이포+원본 자산으로. 나머지 5종도 원본 내용/그래프를 그대로 이식하는 방식으로 전개.
사진/스크린샷 대신 각 케이스의 핵심 메커니즘을 흑백 벡터 다이어그램으로 그린다. 벡터라 무한 선명(이미지 퀄리티 문제 해결), 편집 레퍼런스(차트·다이어그램)와 일치, 흑백 시스템 부합.
- **RaiLink** ✓완료: 두 사람 주간 근무표 겹치기 → 둘 다 쉬는 수요일이 검정으로 켜짐(.caseviz .roster)
- **deokhong.com**: 한 페이지가 퍼지는 도달 다이어그램(예산 0 → 노출) 또는 캠페인 스프레드
- **Samsung Care+**: 한 입구 → 목적별 입구 분기 다이어그램(100+ 마켓 통일)
- **OPENPATH**: A/B 전환 비교 바(기능-우선 vs 체험-우선 → 3.4×)
- **Genesis**: 법적 문구 모듈을 골라 조립하는 구성 다이어그램
- **Korail-talk**: 한꺼번에 묻기 → 하나씩 묻기(점진적 노출) 플로우, 스캔패스 대비

## 이미지 화질 해결법 (확정 워크플로)
저해상 스크린샷 재사용 금지. **라이브 앱/사이트를 헤드리스 크롬 고DPR로 새 캡처** 후 기기부분만 크롭.
- 예: `chrome --headless=new --force-device-scale-factor=2 --window-size=1440,900 --screenshot railink.app` → 데스크톱 폭에서 폰 전체가 온전+선명(2x). 모바일 폭은 폰이 잘리므로 데스크톱 폭 권장.
- RaiLink 폰 자산 교체 완료: 326×629 → 731×1520 (redesign/railink-phone.png). 레티나에서 선명.
- 나머지 케이스도 같은 방식으로 원본 화면을 2x+ 재캡처해 교체.

## 내비게이션·IA 통일 (Theo 승인 2026-07-27)
- **문제**: 크롬이 페이지마다 달랐다. 홈=로고 왼쪽+Menu, 케이스=←Work 왼쪽+로고 오른쪽,
  산출물=←Index+Work, 디자인시스템=←Index+Deliverables. 로고 자리가 좌우로 오갔고,
  메뉴가 홈에만 있어 다른 페이지에선 내가 그때그때 심은 임의 링크로만 이동했다.
- **규칙**: 왼쪽 끝 = 로고(홈), 오른쪽 끝 = Menu, 전 페이지 동일.
  계층이 있는 케이스만 로고 옆에 `Work /`(상위 링크 겸 위치 표시). 산출물·디자인시스템은 Work의 형제라 붙이지 않는다.
- **구현**: `nav.css` + `nav.js`를 9개 페이지가 공유. 메뉴 마크업도 동일. 현재 위치는 nav.js가 표시(케이스는 Work를 현재로).
  홈에 있던 크롬·메뉴 CSS/JS는 nav.*로 옮겼고 case.css의 .chrome도 제거(중복 제거).
- **문서 간 전환**: `@view-transition{navigation:auto}` + ::view-transition-old/new 애니메이션으로
  홈의 셔터와 같은 결. 미지원 브라우저는 즉시 이동(깨지지 않음).
- 케이스 하단 Next(형제 순회)는 그대로. 위로 가는 동선과 옆으로 가는 동선은 다르다.

## 히어로 카드 비주얼 (Theo 승인 2026-07-27)
- **판단: 카드는 '임팩트(수치)'가 아니라 '인식(무슨 프로젝트인지)'을 맡는다.** 이유 셋:
  (1) 히어로의 목표는 설득이 아니라 케이스를 열게 하는 것. 수치는 답을 줘버려 클릭 이유를 없앤다.
  (2) 임팩트는 Work 목록 한 줄과 케이스 15초 요약·수치 밴드에 이미 있다. 카드까지 수치면 세 번 중복.
  (3) 회전+블러+짧은 노출이라 읽는 자리가 아니라 알아보는 자리다.
- **카드 규격(16vw·1.24·4000·-40%·0.17·1vh·1.5°)은 불변.** 정사각 안쪽만 바꿨다.
- 구현: 720×720 정사각 자산 6종(card-*.png/jpg)을 새로 굽고, 이미지는 카드 **가운데 띠(plate)** 에만.
  화면 캡처에도 글자가 있어 카피와 겹치므로 위(태그)·아래(이름)는 검은 여백으로 비운다.
- 크롭 기준: RaiLink=겹쳐진 캘린더 / deokhong=후보 사진 / OPENPATH=노란 강의 카드 /
  Samsung=핀 꽂힌 세계지도(100+ 마켓, 밝기 2.3·대비 1.5 보정) / Genesis=Summary 화면기획 / Korail=히트맵
- 6장 한눈에 점검하는 법: home.html의 .card/.ring 규칙과 카드 마크업만 뽑아 평면 배치한 임시 페이지를 만든다
  (캐러셀은 헤드리스에서 자동 회전하지 않아 정면 카드만 찍힌다). CSS 추출 시 **주석을 먼저 제거**할 것.
- 남은 것: 정면 카드에만 수치 하나 얹기(앞/뒤 역할 분리)는 아직 미적용.

## 그리드 원점 일관 규칙 (Theo 지적 2026-07-27)
메타 라벨 = col 1–2. **본문·요약값·수치밴드·구분선 등 모든 콘텐츠 = col 3부터, 페이지 여백(var(--side)) 안에서 정렬.** 수치가 col1(맨 왼쪽)에서 시작해 본문보다 튀거나, 구분선이 화면 끝까지 풀블리드로 나가지 않게. (grid.css/케이스 공통 적용 대상)

## 원칙

- 매 단계 로컬(localhost)로 Theo 확인 후 다음 단계
- 인터랙션 폴리시 원칙 유지: pointer-down 즉시 반응, hover는 정밀 포인터만, reduced-motion 존중
- 실측 기반: 레퍼런스와 다르면 스크린샷 픽셀로 비교
- 조정 슬라이더 패널로 Theo가 직접 수치 확정하는 방식 우선
